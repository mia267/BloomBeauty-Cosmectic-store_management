<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between">
    <h1 class="flex-1 font-bold text-3xl">Product_category</h1>

    <div class="flex-wrap lg:flex items-center lg:space-x-3">
      <%= link_to product_categories_path, class: button_class(theme: "transparent") do %>
      All Product_categories
      <% end %>
      <%= link_to edit_product_category_path(@product_category), class: button_class(theme:"primary") do %>
      Edit
      <% end %>
    </div>
  </div>

  <ul class="text-slate-700">
      <li>
        <p class="font-semibold">Name:</p>
        <p><%= @product_category.name %></p>
      </li>
      <li>
        <p class="font-semibold">Description:</p>
        <p><%= @product_category.description %></p>
      </li>
  </ul>
</div>
